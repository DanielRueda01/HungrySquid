using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class movRandomPez : MonoBehaviour
{
    float nextXPoint;
    // Start is called before the first frame update
    void Start()
    {
        //float x = Random.Range(-1f, 1f);
        //Rigidbody2D.AddForce(new Vector2(x, 0f * 100f) * 100f, ForceMode2D.Impulse);
    }

    // Update is called once per frame
    void Update()
    {
        



    }
}
